{
  "name": "@brooswit/thatch",
  "version": "0.6.0",
  "description": "Elysia plugin: a central HTTP MCP server with named connections and a channel for pushing messages into Claude Code sessions",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brooswit/thatch.git"
  },
  "homepage": "https://github.com/brooswit/thatch#readme",
  "bugs": {
    "url": "https://github.com/brooswit/thatch/issues"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing/index.d.ts",
      "import": "./dist/testing/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "bun run scripts/load/generate.ts",
    "build": "bun build src/index.ts src/testing/index.ts --outdir dist --target bun --sourcemap=external --external elysia --external @modelcontextprotocol/sdk --external zod && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "test:unit": "bun test test/unit test/load && bun run scripts/coverage/gate.ts",
    "test:live": "MCP_LIVE=1 bun test test/live",
    "release:check": "bun run scripts/release/check.ts",
    "check": "bun run generate && bun run typecheck && bun test test/unit test/load && bun run scripts/coverage/gate.ts && bun run scripts/verify-generated-is-committed.ts"
  },
  "peerDependencies": {
    "elysia": ">=1.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "elysia": "^1.4.29",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1"
  }
}
